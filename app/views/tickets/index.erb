<% @tickets.each do |ticket| %>
<div>
    <h3><%= ticket.title %></h3>
    <h4><%= ticket.description %></h4>
    <h5>Submitter: <%= ticket.user.full_name %> -- Owner: <%= User.find_by_id(ticket.user_admin_id).full_name %></h5>
</div>
<% end %>